# Model Artifacts

Dokumentasikan model yang sudah dievaluasi di sini.

Large weights seperti `.pt`, `.onnx`, `.engine`, dan `.tflite` di-ignore dari Git biasa. Catat storage/artifact URL, model version, metrics, dataset version, dan export format.
